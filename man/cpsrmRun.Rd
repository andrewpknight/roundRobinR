% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{cpsrmRun}
\alias{cpsrmRun}
\title{Deprecated: use \code{\link{cpsrm_run}} instead}
\usage{
cpsrmRun(
  dv,
  actor.id,
  group.id,
  actor.dummies,
  partner.dummies,
  data,
  course.id = NULL,
  zero.rho = FALSE,
  zero.actor = FALSE,
  zero.partner = FALSE,
  zero.group = FALSE,
  zero.course = FALSE,
  weight.partners = TRUE,
  fixed.effects = NULL,
  stage1.maxit = 1000L,
  stage2.maxit = 1000L,
  se.method = c("hessian", "none"),
  rho.boundary = 0.99,
  verbose.every = 50L,
  ...
)
}
\arguments{
\item{dv}{string; name of the outcome variable}

\item{actor.id}{string; name of the actor identifier variable}

\item{group.id}{string; name of the group identifier variable}

\item{actor.dummies}{character vector of actor dummy column names}

\item{partner.dummies}{character vector of partner dummy column names}

\item{data}{a \code{data.frame}}

\item{course.id}{string or \code{NULL}; higher-level clustering variable}

\item{zero.rho}{logical; fix actor-partner correlation at zero}

\item{zero.actor}{logical; fix actor variance at zero}

\item{zero.partner}{logical; fix partner variance at zero}

\item{zero.group}{logical; fix group variance at zero}

\item{zero.course}{logical; fix course/location variance at zero}

\item{weight.partners}{logical; weight partner dummies}

\item{fixed.effects}{character vector or \code{NULL}}

\item{stage1.maxit}{integer; stage 1 iterations}

\item{stage2.maxit}{integer; stage 2 iterations}

\item{se.method}{character; \code{"hessian"} or \code{"none"}}

\item{rho.boundary}{numeric; boundary threshold}

\item{verbose.every}{integer; verbose print frequency}

\item{...}{additional arguments passed to \code{\link{cpsrm_run}}}
}
\value{
see \code{\link{cpsrm_run}}
}
\description{
\code{cpsrmRun} has been renamed to \code{cpsrm_run}.
Key argument changes: \code{course.id} -> \code{location_id};
\code{actor.dummies} -> \code{actor_dummies};
\code{partner.dummies} -> \code{partner_dummies};
\code{zero.rho} -> \code{zero_rho}; \code{zero.actor} -> \code{zero_actor};
\code{zero.partner} -> \code{zero_partner};
\code{zero.group} -> \code{zero_group};
\code{zero.course} -> \code{zero_location};
\code{weight.partners} -> \code{weight_partners};
\code{fixed.effects} -> \code{fixed_effects};
\code{stage1.maxit} -> \code{stage1_maxit};
\code{stage2.maxit} -> \code{stage2_maxit};
\code{se.method} -> \code{se_method};
\code{rho.boundary} -> \code{rho_boundary};
\code{verbose.every} -> \code{verbose_every}.
}
